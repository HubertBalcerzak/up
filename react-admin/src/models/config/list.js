import React from 'react'
import {
  List,
  Datagrid,
  TextField,
  EditButton,
  Pagination
} from 'react-admin'

import { Card, CardContent, withStyles } from '@material-ui/core';


export default (props) => (
  <Card>
    <CardContent>
      asd
    </CardContent>
  </Card>
)
